# GitHub 上传指南

## ✅ 已完成的工作

1. ✅ Git仓库已初始化
2. ✅ 所有文件已添加到Git
3. ✅ 已创建初始提交
4. ✅ Git用户信息已配置（邮箱：rsjscu@163.com）

## 📋 上传到GitHub的步骤

### 方法一：使用GitHub网页创建仓库（推荐）

1. **登录GitHub**
   - 访问 https://github.com
   - 使用您的账号登录

2. **创建新仓库**
   - 点击右上角的 "+" 按钮，选择 "New repository"
   - 仓库名称建议：`Ourmind` 或 `ourmind`
   - 描述：`文档思维导图生成器 - AI驱动的智能文档分析和思维导图生成工具`
   - 选择 **Public**（公开）或 **Private**（私有）
   - **不要**勾选 "Initialize this repository with a README"（因为我们已经有了）
   - 点击 "Create repository"

3. **获取仓库URL**
   - 创建后，GitHub会显示仓库URL，类似：
     ```
     https://github.com/your-username/Ourmind.git
     ```

4. **添加远程仓库并推送**
   在项目目录下运行以下命令（将 `your-username` 替换为您的GitHub用户名）：

   ```bash
   cd /home/rsj/Ourmind
   
   # 添加远程仓库（替换为您的实际仓库URL）
   git remote add origin https://github.com/your-username/Ourmind.git
   
   # 推送代码到GitHub
   git branch -M main
   git push -u origin main
   ```

   如果提示输入用户名和密码：
   - 用户名：您的GitHub用户名
   - 密码：需要使用 **Personal Access Token**（不是GitHub密码）
     - 如何获取Token：Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate new token
     - 权限选择：至少勾选 `repo` 权限

### 方法二：使用GitHub CLI（如果已安装）

```bash
cd /home/rsj/Ourmind
gh repo create Ourmind --public --source=. --remote=origin --push
```

### 方法三：使用SSH（如果已配置SSH密钥）

1. 在GitHub创建仓库（同方法一步骤1-3）

2. 使用SSH URL添加远程仓库：
   ```bash
   cd /home/rsj/Ourmind
   git remote add origin git@github.com:your-username/Ourmind.git
   git branch -M main
   git push -u origin main
   ```

## 🎬 关于演示视频

演示视频文件 `pic/Ourmind_demo.mp4` 已经包含在仓库中。GitHub会自动显示视频文件，访问者可以直接在GitHub上观看。

如果视频文件太大（超过100MB），GitHub可能会警告，但5.7MB的视频文件完全没问题。

## 📝 后续更新代码

以后如果需要更新代码，使用以下命令：

```bash
cd /home/rsj/Ourmind

# 添加修改的文件
git add .

# 提交更改
git commit -m "描述你的更改"

# 推送到GitHub
git push
```

## 🔍 验证上传

上传成功后，访问您的GitHub仓库页面，应该能看到：
- ✅ README.md 文件（包含项目说明和演示视频链接）
- ✅ 所有源代码文件
- ✅ 演示视频文件 `pic/Ourmind_demo.mp4`

## ❓ 常见问题

### Q: 提示需要认证怎么办？
A: GitHub已经不再支持密码认证，需要使用Personal Access Token：
1. 访问 https://github.com/settings/tokens
2. 点击 "Generate new token (classic)"
3. 勾选 `repo` 权限
4. 生成后复制Token，在输入密码时使用这个Token

### Q: 视频文件太大无法上传？
A: 如果视频超过100MB，可以考虑：
- 使用Git LFS（Large File Storage）
- 将视频上传到其他平台（如YouTube、Bilibili）并在README中链接
- 压缩视频文件

### Q: 如何更新README中的视频链接？
A: 视频上传后，GitHub会自动生成链接。您可以在README中使用：
```markdown
[观看演示视频](pic/Ourmind_demo.mp4)
```

## 📞 需要帮助？

如果遇到任何问题，请检查：
1. Git配置是否正确：`git config --list`
2. 远程仓库是否正确添加：`git remote -v`
3. 网络连接是否正常

---

**祝您上传顺利！** 🚀
