# 公网访问方案

让网站可以从任何地方访问，就像一般网站那样。

## 方案一：使用 ngrok（最简单，推荐）⭐

### 优点
- ✅ 免费（有使用限制）
- ✅ 无需配置，一键启动
- ✅ 自动提供HTTPS
- ✅ 支持自定义域名（付费版）

### 安装和使用

1. **安装 ngrok**
   ```bash
   # Linux/Mac
   curl -s https://ngrok-agent.s3.amazonaws.com/ngrok.asc | sudo tee /etc/apt/trusted.gpg.d/ngrok.asc >/dev/null
   echo "deb https://ngrok-agent.s3.amazonaws.com buster main" | sudo tee /etc/apt/sources.list.d/ngrok.list
   sudo apt update && sudo apt install ngrok
   
   # 或直接下载
   wget https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-linux-amd64.tgz
   tar -xzf ngrok-v3-stable-linux-amd64.tgz
   sudo mv ngrok /usr/local/bin/
   ```

2. **注册并获取token**（免费）
   - 访问 https://dashboard.ngrok.com/signup
   - 注册账号后获取authtoken
   - 配置token：
     ```bash
     ngrok config add-authtoken YOUR_AUTH_TOKEN
     ```

3. **使用启动脚本**
   ```bash
   ./start_with_ngrok.sh
   ```
   脚本会自动启动服务并创建公网链接。

### 限制
- 免费版：每次启动URL会变化
- 免费版：有连接数限制
- 免费版：有流量限制

---

## 方案二：使用 Cloudflare Tunnel（完全免费）⭐

### 优点
- ✅ 完全免费
- ✅ 无流量限制
- ✅ 自动HTTPS
- ✅ 可以绑定自定义域名

### 安装和使用

1. **安装 cloudflared**
   ```bash
   # Linux
   wget https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64
   chmod +x cloudflared-linux-amd64
   sudo mv cloudflared-linux-amd64 /usr/local/bin/cloudflared
   ```

2. **使用启动脚本**
   ```bash
   ./start_with_cloudflare.sh
   ```
   首次运行会要求登录Cloudflare账号。

---

## 方案三：部署到云服务器

### 需要
- 云服务器（阿里云、腾讯云、AWS等）
- 域名（可选，但推荐）
- 公网IP

### 步骤

1. **购买云服务器**
2. **在服务器上部署应用**
3. **配置Nginx反向代理**
4. **配置HTTPS（Let's Encrypt）**
5. **绑定域名**

详细步骤请参考：`部署到服务器.md`

---

## 方案四：使用 frp（需要自己的服务器）

如果你有自己的公网服务器，可以使用frp进行内网穿透。

---

## 推荐方案对比

| 方案 | 难度 | 成本 | 稳定性 | 推荐度 |
|------|------|------|--------|--------|
| ngrok | ⭐ 简单 | 免费/付费 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| Cloudflare Tunnel | ⭐⭐ 中等 | 免费 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 云服务器 | ⭐⭐⭐⭐ 复杂 | 付费 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| frp | ⭐⭐⭐ 中等 | 需要服务器 | ⭐⭐⭐⭐ | ⭐⭐⭐ |

## 快速开始

**推荐使用 ngrok 或 Cloudflare Tunnel**，我已经创建了自动化脚本，直接运行即可！






